# Pisah vs Gabung — audit harness

Drives the real page in headless Chromium (CDN libs stubbed, no network) and
mimics a user: reads KPIs, switches to Husband+Wife input mode, edits brackets.
Cross-checks the KPIs against an independent reimplementation of the PPh-21
progressive schedule.

Run: `node run.mjs`
